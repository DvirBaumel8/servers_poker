/**
 * Timing constants for animations and UI behaviors.
 * Centralizing these values makes them easier to adjust and test.
 */

/** Timer interval in milliseconds (1 second) */
export const TIMER_INTERVAL_MS = 1000;

/** Action feed item display duration */
export const ACTION_FEED_DISPLAY_MS = 3000;

/** Copy to clipboard feedback duration */
export const COPY_FEEDBACK_MS = 2000;

/** Winner animation chip phase duration */
export const WINNER_CHIP_PHASE_MS = 1200;

/** Winner animation total duration */
export const WINNER_ANIMATION_MS = 4000;

/** Active bots polling interval */
export const ACTIVE_BOTS_POLL_MS = 30000;

/** Tournament card countdown interval */
export const TOURNAMENT_COUNTDOWN_MS = 1000;

/** Success message auto-dismiss duration */
export const SUCCESS_MESSAGE_MS = 5000;

/** Bot profile data refresh interval */
export const BOT_PROFILE_POLL_MS = 10000;

/** Password reset redirect delay */
export const RESET_PASSWORD_REDIRECT_MS = 2000;

/** Tournament detail refresh interval */
export const TOURNAMENT_DETAIL_POLL_MS = 5000;

/** Tournament list refresh interval */
export const TOURNAMENT_LIST_POLL_MS = 10000;

/** Analytics flush timeout */
export const ANALYTICS_FLUSH_MS = 5000;
