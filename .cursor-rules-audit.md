# Cursor Rules Audit & Recommendations

## Summary
- **Total rules:** 28 files
- **Status:** 4 files should be **DELETED** (deprecated systems), 6 need **UPDATES** to remove Monster Army references
- **Healthy rules:** 18 rules are relevant and current

---

## 🔴 DELETE (Deprecated Systems)

### 1. `bug-to-monster.mdc` — **DELETE**
**Reason:** Monster Army QA system is deprecated. References deleted files:
- `npm run monsters:learn` (script no longer exists)
- `tests/qa/monsters/pending-learnings.md` (deleted)
- Monster references like `browser-qa`, `invariant-monster` (deleted)

**Action:** Remove entirely. Functionality replaced by:
- `npm run test:poker` (game invariant testing)
- `bash scripts/detect-ui-bugs.sh` (Gemini UI bug detection)

---

### 2. `bug-to-monster-workflow.mdc` — **DELETE**
**Reason:** Monster Army workflow is obsolete. References:
- `tests/qa/monsters/data/bug-retrospectives.json` (doesn't exist)
- Monster improvement workflow (replaced by simpler test-per-fix rule)
- `npm run monsters:*` commands (no longer valid)

**Action:** Remove entirely. Replaced by memory rule: "Add tests when fixing bugs"

---

### 3. `bug-report-cleanup.mdc` — **DELETE**
**Reason:** References deleted files:
- `docs/reports/**/*.md` (entire folder deleted)
- `docs/MONSTERS_ISSUES.md` (deleted)
- `docs/qa_report.md` (deleted)

**Action:** Remove entirely. No active bug report cleanup needed.

---

### 4. `metrics-observability.mdc` — **DELETE**
**Reason:** Entire metrics module was removed. References:
- `src/modules/metrics/` (all files deleted: metrics.service.ts, metrics.controller.ts, metrics.module.ts)
- `InjectMetric` decorator (no longer exists)
- `/metrics` endpoint (removed)

**Action:** Remove entirely. No prometheus metrics in current architecture.

---

## 🟡 UPDATE (Minor Fixes Needed)

### 1. `docs-sync.mdc` — **UPDATE**
**Issues:**
- References deleted Monster config files:
  - `tests/qa/monsters/visual-monster/visual-monster.config.ts` (deleted)
  - `tests/qa/monsters/browser-monster/scenarios.ts` (deleted)
  - `tests/qa/monsters/browser-monster/browser-qa-monster.ts` (deleted)
  - 12+ other deleted monster files in the "Monster Config Updates" table
- References deleted docs:
  - `docs/KNOWLEDGE.md` (doesn't exist)
  - `docs/DATA_DICTIONARY.md` (doesn't exist)

**Fix:**
1. Remove entire "Monster Config Updates" section (lines 40-59)
2. Remove Monster-specific entries from "QA Monster Updates" table (lines 62-70)
3. Keep API/entity/migration/route update guidance

---

### 2. `logging.mdc` — **UPDATE**
**Issues:**
- References deleted file: `src/utils/logger.ts` (deleted)
- Frontend logger code example shows old import path

**Fix:**
- Remove frontend logger example (lines 36-44) OR check if logger still exists
- Update to current utility location if it exists

---

### 3. `performance.mdc` — **UPDATE**
**Issues:**
- References lazy loading with `AdminAnalytics` page (deleted)
- WebSocket emit pattern suggests old API

**Fix:**
- Update lazy loading example to a page that still exists
- Verify WebSocket pattern matches current `games.gateway.ts`

---

### 4. `test-on-change.mdc` — **CHECK/UPDATE**
**Issues:**
- Need to verify if this file actually exists (not in list provided)
- If it exists, check for Monster Army references

**Fix:**
- Either find and update, or it may already be gone

---

### 5. `pr-review.mdc` — **CHECK**
**Issues:**
- Need to verify content (not in list provided)
- May reference deleted testing systems

**Fix:**
- Review when accessed

---

### 6. `coverage-exclusions.mdc` — **UPDATE**
**Issues:**
- References `src/simulation/**` and `src/workers/**` as exclusions (both deleted)
- Not a major issue, but outdated

**Fix:**
- Remove references to deleted directories
- Simplify exclusion patterns

---

## ✅ KEEP (Relevant & Current)

These rules are healthy and should remain:

1. **api-conventions.mdc** — Current, reflects actual API structure
2. **code-style.mdc** — General coding style guidelines
3. **database-patterns.mdc** — Still relevant for TypeORM patterns
4. **documentation.mdc** — References current docs structure
5. **environment-config.mdc** — Still valid for config management
6. **error-handling.mdc** — Current error handling patterns
7. **event-patterns.mdc** — Socket.IO patterns are current
8. **git-workflow.mdc** — Git conventions still apply
9. **migrations.mdc** — Database migrations still used
10. **nestjs-modules.mdc** — Current NestJS structure
11. **react-patterns.mdc** — Frontend patterns still valid
12. **security.mdc** — Security best practices still apply
13. **shared-utilities.mdc** — Shared code patterns relevant
14. **tech-debt-management.mdc** — Tech debt management still needed
15. **testing.mdc** — Test structure still valid
16. **websocket-patterns.mdc** — Current for games.gateway.ts
17. **pr-workflow.mdc** — Still enforces local CI checks
18. **pr-review.mdc** — (if it exists and is relevant)

---

## Recommendations Summary

| Action | Count | Files |
|--------|-------|-------|
| DELETE | 4 | bug-to-monster*, bug-report-cleanup, metrics-observability |
| UPDATE | 6 | docs-sync, logging, performance, coverage-exclusions, + 2 more |
| KEEP | 18 | All others |

**Estimated effort:** 30 minutes to clean up

**Priority:** Medium — These deletions won't break anything but will reduce confusion about deprecated systems.
