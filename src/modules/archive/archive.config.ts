export const ARCHIVE_CONFIG = {
  CRON_SCHEDULE: "0 4 * * *",
  LOCK_KEY: "lock:archive:daily",
  LOCK_TTL_MS: 600_000,
  DEFAULT_RETENTION_DAYS: 7,
  DEFAULT_DELETE_BATCH_SIZE: 1000,
  MAX_TOURNAMENTS_PER_RUN: 50,
  SERIALIZATION_PAGE_SIZE: 500,
} as const;
